# v1.9.0
### Match Statements
- The `when` function has been updated to narrow the object based on the pattern provided.
  > <i>This can sometimes cause issues when matching properties on a class instance. In such cases you can resort to the old `when` function which is now called `when$`.</i>
